'use client'

import React from 'react'
import { StoreMobileHeader } from './store-mobile-header'

/**
 * RegistryMobileNavigation: Wrapper for the canonical StoreMobileHeader within the Registry context.
 */
export function RegistryMobileNavigation() {
  return <StoreMobileHeader />
}
